#include <stdio.h>
int main() {
    int n=10, a = 0, b = 1,c;
  printf("Fibonacci Series: %d, %d, ", a,b);
    for (int i = 1; i <= n; ++i) {
       c=a+b;
        printf("%d, ", c);
       a=b;
       b=c;
    }
    return 0;
}
